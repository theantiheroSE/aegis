# Dependencies
node_modules/

# Secrets & runtime state — NEVER commit
config.json
state.json

# Logs (contain run history, paths, error messages)
logs/
backup-*.log
*.log

# Local project memory — internal dev notes, not for the public repo
AGENTS.md

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
